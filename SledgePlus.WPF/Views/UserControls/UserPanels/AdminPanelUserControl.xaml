<UserControl x:Class="SledgePlus.WPF.Views.UserControls.UserPanels.AdminPanelUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SledgePlus.WPF.ViewModels.UserControls.UserPanels.AdminPanels"
             xmlns:adminPanels="clr-namespace:SledgePlus.WPF.Views.UserControls.UserPanels.AdminPanels"
             Background="{DynamicResource MaterialDesignDarkBackground}"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             mc:Ignorable="d"
             d:DesignHeight="270" d:DesignWidth="780">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:UsersTableViewModel}">
            <adminPanels:UsersTableUserControl />
        </DataTemplate>
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal">
        <StackPanel Margin="10">
            <Button Content="Пользователи" Width="150" Command="{Binding ToAdminUsersTableCommand}" Cursor="Hand" />
        </StackPanel>
        <ContentControl Content="{Binding CurrentPanel}" Width="610" />
    </StackPanel>
</UserControl>